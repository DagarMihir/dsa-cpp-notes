#include <iostream>
using namespace std;

int main(){

/*
    int a = 123;
    cout << a << endl;

    char b = 'a'; //only single character i.e. 'ab' is invalid
     cout << b << endl;

    bool bl = true;
    cout << bl << endl;

    float f = 1.2;
    cout << f << endl;

    double d = 1.23;
    cout << d << endl;

    int size = sizeof(a);
    cout << "Size of a (int) is : " << size << endl;



    // Type Casting

    int a = 'a';
    cout << a << endl;

    char ch = 98;
    cout << ch << endl;

    char ch1 = 123456;
    cout << ch1 << endl;

    //here 123456 is out of bound for char data type, thus it prints @ which is 64 in ascii, it stores the last bits in the memory.

    // Signed(Default) and Unsigned int (Only stores +ve numbers).


*/

    unsigned int a = -112;

    cout << a << endl;

}